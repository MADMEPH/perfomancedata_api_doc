# 🚀 Инструкция по деплою на GitHub Pages

## Шаг 1: Создайте репозиторий на GitHub

1. Перейдите на [GitHub](https://github.com)
2. Нажмите **"New repository"** (зелёная кнопка)
3. Заполните данные:
   - **Repository name**: `perion-api-docs` (или любое другое имя)
   - **Description**: `Perion Performance Data API Documentation`
   - **Visibility**: Public или Private (оба работают)
   - ❌ **НЕ создавайте** README, .gitignore, license (уже есть локально)
4. Нажмите **"Create repository"**

## Шаг 2: Обновите конфигурацию

Откройте файл `docs/.vitepress/config.mjs` и измените строку:

```javascript
base: '/perion-api-docs/',  // Замените на название вашего репозитория
```

Если ваш репозиторий называется `perion-api-docs`, то оставьте как есть.
Если другое имя, например `api-documentation`, то:

```javascript
base: '/api-documentation/',
```

## Шаг 3: Закоммитьте изменения

```powershell
git add .
git commit -m "Update base path for GitHub Pages"
```

## Шаг 4: Подключите удалённый репозиторий

Замените `your-username` на ваш GitHub username:

```powershell
git remote add origin https://github.com/your-username/perion-api-docs.git
git branch -M main
```

## Шаг 5: Отправьте код на GitHub

```powershell
git push -u origin main
```

## Шаг 6: Настройте GitHub Pages

1. Перейдите в **Settings** вашего репозитория
2. В левом меню выберите **Pages**
3. В разделе **Source** выберите:
   - **Source**: GitHub Actions
4. Сохраните изменения

## Шаг 7: Дождитесь деплоя

1. Перейдите во вкладку **Actions** вашего репозитория
2. Дождитесь завершения деплоя (зелёная галочка ✅)
3. Документация будет доступна по адресу:
   ```
   https://your-username.github.io/perion-api-docs/
   ```

## 🎉 Готово!

Теперь при каждом push в ветку `main` документация будет автоматически обновляться!

---

## 🔧 Команды для разработки

### Локальная разработка
```powershell
npm run docs:dev
```
Откроет http://localhost:5173/perion-api-docs/

### Сборка
```powershell
npm run docs:build
```
Создаст статические файлы в `docs/.vitepress/dist`

### Предпросмотр сборки
```powershell
npm run docs:preview
```

---

## 📝 Обновление документации

1. Внесите изменения в файлы `.md`
2. Проверьте локально: `npm run docs:dev`
3. Закоммитьте и запушьте:
   ```powershell
   git add .
   git commit -m "Update documentation"
   git push
   ```
4. GitHub Actions автоматически задеплоит изменения

---

## 🌍 Структура документации

```
docs/
├── .vitepress/
│   ├── config.mjs          # Конфигурация VitePress
│   └── theme/
│       ├── index.js        # Кастомизация темы
│       └── custom.css      # Стили
├── index.md                # Главная страница (EN)
├── guide/                  # Руководства (EN)
├── api/                    # API справочник (EN)
├── examples/               # Примеры кода (EN)
└── ru/                     # Русская версия
    ├── index.md
    ├── guide/
    ├── api/
    └── examples/
```

---

## 🎨 Кастомизация

### Цвета
Редактируйте `docs/.vitepress/theme/custom.css`

### Навигация
Редактируйте `docs/.vitepress/config.mjs` → `themeConfig.nav` и `themeConfig.sidebar`

### Контент
Редактируйте `.md` файлы в соответствующих папках

---

## ❓ Помощь

- [VitePress Documentation](https://vitepress.dev/)
- [GitHub Pages Documentation](https://docs.github.com/en/pages)
- [GitHub Actions Documentation](https://docs.github.com/en/actions)
